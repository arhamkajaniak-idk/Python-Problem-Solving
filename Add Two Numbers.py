def Sum3Closest(nums, target):
    if len(nums)<3:
        return "Invalid list"
    else:
        for i in range(len(nums)):
            sum=0
            target=input("Enter a number: ")
            sorted(nums)

    